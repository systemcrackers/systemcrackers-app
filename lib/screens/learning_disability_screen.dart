import 'package:flutter/material.dart';

class LearningDisabilityScreen extends StatelessWidget {
  const LearningDisabilityScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}
